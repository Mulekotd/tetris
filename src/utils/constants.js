// Game constants
export const BOARD_WIDTH = 10;
export const BOARD_HEIGHT = 20;
export const BLOCK_SIZE = 30;

// Game timing
export const BASE_DROP_INTERVAL = 675;
export const LOCK_DELAY = 500;

// Input keys
export const KEYS = {
  LEFT: ['a', 'arrowleft'],
  RIGHT: ['d', 'arrowright'],
  DOWN: ['s', 'arrowdown'],
  ROTATE: [' '],
  PAUSE: ['p'],
  RESTART: ['r']
};
